@model GetByIdAddressDto
@{
    ViewData["Title"] = "UpdateAddress";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="row">
    <div class="col">
        <div class="card shadow p-3">
            <div class="card-header border-0">
                <h3 class="mb-0">İletişim bilgileri Güncelle</h3>
            </div>
            <form method="post">
                <input type="hidden" asp-for="@Model.Id" />
                <div class="form-group">
                    <label class="form-label">Url</label>
                    <input type="text" class="form-control" asp-for="@Model.City" placeholder="Şehir" />
                </div>
                <div class="form-group">
                    <label class="form-label">Ikon</label>
                    <input type="text" class="form-control" asp-for="@Model.Country" placeholder="Ülke" />
                </div>
                <div class="form-group">
                    <label class="form-label">Ikon</label>
                    <input type="text" class="form-control" asp-for="@Model.AddressDetail" placeholder="Adres" />
                </div>
                <div class="form-group">
                    <label class="form-label">Ikon</label>
                    <input type="text" class="form-control" asp-for="@Model.PhoneNumber" placeholder="Telefon" />
                </div>
                <div class="form-group">
                    <label class="form-label">Ikon</label>
                    <input type="text" class="form-control" asp-for="@Model.Email" placeholder="Email" />
                </div>
                <div class="form-group">
                    <label class="form-label">Ikon</label>
                    <input type="text" class="form-control" asp-for="@Model.WebSite" placeholder="Web sitesi" />
                </div>
                @if (ViewBag.Errors != null && ViewBag.Errors.Count > 0)
                {
                    <div class="alert alert-danger">
                        @foreach (var item in ViewBag.Errors)
                        {
                            <p>@item</p>

                        }
                    </div>
                }
                <button type="submit" class="btn btn-outline-primary">Kaydet</button>
            </form>

        </div>
    </div>
</div>

